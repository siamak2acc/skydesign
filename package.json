{
  "name": "skydesign-flight-deal-finder",
  "version": "1.0.0",
  "description": "Static flight deal finder website for skydesign.com.au with FTP deployment tooling.",
  "main": "deploy_static_site.js",
  "scripts": {
    "build": "node deploy_static_site.js build",
    "serve": "node deploy_static_site.js serve",
    "deploy": "node deploy_static_site.js deploy"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "basic-ftp": "^5.0.5",
    "dotenv": "^16.4.7"
  }
}
